<li>
  <a href="<%= project_deployments_path(project) %>" class="flex items-center gap-2">
    <%= render "projects/favicon", project: project %>
    <div class="flex flex-col">
      <span class="text-base"><%= highlight(project.name, query, highlighter: '<span class="underline">\1</span>') %></span>
      <span class="text-xs text-base-content/50"><%= project.cluster.name %></span>
    </div>
  </a>
</li>
